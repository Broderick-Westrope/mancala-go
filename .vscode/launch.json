{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch mancala_test",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}/internal/mancala/"
        }
    ]
}
